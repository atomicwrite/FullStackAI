using FullStackAI.ServiceStack.VueSpa;
using FullStackAI.Stack;

namespace FullStackAI.Tests;

public class TestType
{
}

public class Tests
{
    [SetUp]
    public void Setup()
    {
    }

    [Test]
    public void GetStackEndPoint()
    {
        //var ateTest = new ServiceStackVueSpaCreate(new StackType(typeof(TestType)));
        // var output = ateTest.GetStackEndPoint();
        // var fileContents = output.ClassFileContents.Render();
        // var requestObject = output.RequestObjectFileContents.Render();
        // var responseObject = output.ResponseObjectFileContents.Render();
        //Console.WriteLine(fileContents);
        Assert.Pass();
    }
}